---
const { title, value, color = "text-blue-600" } = Astro.props;
---

<div
  class="bg-white shadow-md rounded-4xl p-5 border border-gray-200 flex flex-col"
>
  <p class="text-gray-500 text-sm">{title}</p>
  <p class={`text-2xl font-bold mt-2 ${color}`}>{value}</p>
</div>
